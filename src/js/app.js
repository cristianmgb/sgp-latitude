import 'bootstrap'
import '@fortawesome/fontawesome-free'
import Vue from 'vue'

// App
import '../scss/app'

/**
 * [app description]: Instanciamos Vue en 'app'
 * @type {Vue}
 */
new Vue({
    el: '#app',
    data: {
    	msg: ''
	}
})